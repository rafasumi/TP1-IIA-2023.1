#ifndef SORT_H
#define SORT_H

#include <string>
#include <sstream>
#include <vector>
#include <unordered_map>

std::string search_sort(std::vector<int>& target, std::string& algorithm, int& cost,
                 int& states);

#endif